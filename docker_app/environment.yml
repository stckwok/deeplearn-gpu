name: deeplearn-rtx-wsl
channels:
  - conda-forge
  - defaults
dependencies:
  - python==3.10.0
  - anaconda
  - cudatoolkit==11.2.0
  - cudnn<9.0.0
  - Flask
  - cmake
  - pip
  - pip:
      # - -r requirements.txt
      # - cmake
      - tensorflow-gpu<2.11
      - opencv-python
      - opencv-contrib-python
      - dlib
      # pytest libs
      - pylint
      - pytest
      - pytest-cov
      - click
      - black

prefix: /opt/conda
